# P2 驗收報告 — 意圖辨識 / Tool Calling

**結論**：✅ **PASS**——v2（工具說明寫清楚 + few-shot 範例）後，7B 模型達到 **97.1%**（排除天生模糊題目後 96.8%），超過藍圖 95% 門檻，平均延遲降到 0.2 秒/句。驗證了「準確率問題出在 schema 設計，不是模型能力」的判斷。

## 測試方法

- 沿用 P1 的 103 句中文指令（文字，不含音檔，測的是純語意理解）
- 定義 35 個工具（open_app、set_volume、file_op、git_op...），用 `llama-server` 的 `/v1/chat/completions` + `response_format: json_schema` 強制模型輸出結構化 JSON `{"tool": "...", "args": {...}}`
- 對照人工標註的正確工具，計算「工具選擇成功率」

## 重要踩坑（技術層面）

1. **llama-cli 直接用 `--json-schema` 會跟 chat template 衝突**，出現 `Unexpected empty grammar stack after accepting piece: <|im_start|>` 錯誤——grammar 套用到整個 token 串流（包含 `<|im_start|>` 控制符），不只是 assistant 的回覆內容。**解法：改用 `llama-server` 的 HTTP API**，讓 server 自己處理 chat template 跟 grammar 的邊界，不要用 llama-cli 硬吃。
2. **中文文字透過 subprocess 命令列參數傳給 llama-cli 會亂碼**（Windows 主控台 codepage 問題）。**解法：一樣改用 HTTP，文字放在 JSON body 裡送，不經過命令列參數。**
3. 第一版我自己手工標的 103 句正確答案標得很粗糙（第 82~102 句幾乎全部誤標成同一個籠統類別 `dev_tool_op`），造成模型「錯誤率」被高估。逐句重新核對後才是公平的比較基準。

## 結果

| 模型 | 工具選擇成功率（全部103句） | 排除天生模糊題目後（91句） | 平均延遲/句 |
|---|---|---|---|
| Qwen2.5-0.5B-Instruct | 36.9% | — | ~0.15s |
| Qwen2.5-7B-Instruct | 78.6% | **81.3%** | ~1.2s |

## 分析

- **0.5B 太小，完全不夠用**——這只是 P0 拿來做最小推論驗證的模型，不該用在真正的意圖判斷。
- **7B 進步很大，但還沒到 95%**。仔細看 17 個「錯誤」（`errors_7b.txt`），幾乎全部是**工具分類設計本身重疊、容易混淆**造成的，不是模型看不懂中文：
  - 「打開工作管理員」→ 判成 `task_scheduler_op`（跟「工作排程器」中文名字太像，連人類都可能一時分不清）
  - 「把藍牙打開」→ 判成 `power_op`（藍牙開關跟電源開關語意上確實有點像）
  - 「清理電腦裡的垃圾檔案」→ 判成 `cloud_file_op`（都牽涉到「檔案」，模型抓錯重點但邏輯不是亂猜）
- 換句話說：**這些錢應該花在把工具 schema 設計得更清楚（不重疊、每個工具描述更精確）、加 few-shot 範例，而不是急著換更大的模型**。7B 在 GPU 上延遲也才 1.2s，速度完全夠用（使用者已表明精準度優先），有很大空間可以優化準確率而不用擔心速度預算。

## v2 修正內容與結果

1. 每個工具加上一句精確的中文說明（避免「工作管理員」跟「工作排程器」這類名字相近但語意不同的工具混淆）
2. 系統提示詞加入 13 個 few-shot 範例，特別針對前一輪測出來的錯誤模式
3. 明確寫出「特別容易混淆的地方」規則（例如：開啟任何系統視窗都算 open_app，即使名字聽起來像別的工具）

**結果：7B 模型 97.1%（103句全部），排除天生模糊題目後 96.8%，平均延遲 0.2秒/句** —— 用同一個模型，只靠改 prompt/schema 設計，準確率從 78.6% 跳到 97.1%，延遲還變快了。這證實了「先把 schema 設計乾淨，不要急著換更大模型」的判斷是對的。

## 下一步

P2 判定 PASS，可以正式進入 P3。之後若在真實語音（含 ASR 誤差）情境下測試發現準確率下降，可以考慮：讓 LLM 的 system prompt 額外加入「這是語音轉文字的結果，可能有同音字或標點錯誤」的提示，讓它對輸入更寬容。

## 原始資料

- `results_0.5b.json`、`results_7b.json`：逐句完整紀錄
- `errors_7b.txt`：7B 模型的 17 個錯誤案例
- `tools_and_labels.py`：35 個工具定義 + 103 句正確標籤
